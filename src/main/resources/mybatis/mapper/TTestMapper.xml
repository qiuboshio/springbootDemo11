<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qzp.demo.mapper.TestDao">
    <update id="update" parameterType="com.qzp.demo.entity.TTest">
        update t_test set name=#{name,jdbcType=VARCHAR} , age=#{age,jdbcType=INTEGER} ,email=#{email,jdbcType=VARCHAR} where id=#{id}
    </update>
</mapper>